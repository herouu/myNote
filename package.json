{
  "name": "mynote",
  "version": "1.0.0",
  "description": "myNote monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n worker,app -c blue,green \"npm run dev --prefix my-app-worker\" \"npm run dev --prefix my-app\"",
    "dev:worker": "npm run dev --prefix my-app-worker",
    "dev:app": "npm run dev --prefix my-app",
    "install:all": "npm install --prefix my-app && npm install --prefix my-app-worker"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
